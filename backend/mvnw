#!/bin/sh
set -e
BASEDIR=$(cd "$(dirname "$0")" && pwd)
exec java -classpath "$BASEDIR/.mvn/wrapper/maven-wrapper.jar" \
  "-Dmaven.multiModuleProjectDirectory=$BASEDIR" \
  org.apache.maven.wrapper.MavenWrapperMain "$@"
